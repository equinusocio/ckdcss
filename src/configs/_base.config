/* Disable motions when user enable
 * Reduce Motion flag (Mac/Safari only) */
@media (prefers-reduced-motion) {
  [data-ckd] {
    animation: unset !important;
    transition: none !important;
  }
}
