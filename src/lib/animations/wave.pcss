@import '../../configs/_variables.config';
@import '../../configs/_base.config';

[data-ckd='wave'] {
  &:checked {
    animation: wave 0.6s $standard-curve infinite;
  }
}

@keyframes wave {
  50% {
    transform:
      skewY(2deg)
      skewX(-2deg)
      scale(1.16);
  }
}
